// import{ }

const initialState = {};

const truckUserReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default truckUserReducer;
