// import{ }

const initialState = {};

const truckOperatorReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default truckOperatorReducer;
