// import{ }

const initialState = {};

const truckDinerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default truckDinerReducer;
